<Window x:Class="CorelTileStudio.Views.PromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CorelTile Studio"
        Width="380" Height="180"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Border Background="{DynamicResource BgDeep}"
            BorderBrush="{DynamicResource BorderStrongBrush}"
            BorderThickness="1" CornerRadius="8" Padding="18">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="TitleText" Grid.Row="0"
                       Foreground="{DynamicResource TextBrush}"
                       FontWeight="Bold" FontSize="14"/>
            <TextBlock x:Name="LabelText" Grid.Row="1"
                       Foreground="{DynamicResource TextMutedBrush}"
                       FontSize="11" Margin="0,8,0,4"/>
            <TextBox x:Name="InputBox" Grid.Row="2" VerticalAlignment="Top"/>
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                <Button Content="Cancelar" Style="{StaticResource GhostButton}"
                        Click="Cancel_Click" Margin="0,0,8,0"/>
                <Button Content="Aceptar" Style="{StaticResource PrimaryButton}"
                        Click="Ok_Click" IsDefault="True"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
